class Solution {
public:
    void reverseString(vector<char>& s) {
        vector<char> n;
        for(int i=0;i<s.size();i++){
            n.push_back(s[i]);
        }
        for(int i=0;i<s.size();i++){
            s[i]=n[s.size()-i-1];
        }
    }
};
